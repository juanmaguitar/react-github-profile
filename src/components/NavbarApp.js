import React from 'react';
import { Navbar } from 'react-bootstrap'

const NavbarApp = () => (
  <Navbar>
    <Navbar.Header>
      <Navbar.Brand>
        <a href="#">Github Profile Finder 🔎</a>
      </Navbar.Brand>
    </Navbar.Header>
  </Navbar>
);

export default NavbarApp
